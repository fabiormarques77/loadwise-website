# Styles

The approved global visual system is implemented in `app/globals.css` and `app/language.css`. This directory is reserved for future isolated style modules without changing the approved design system.
